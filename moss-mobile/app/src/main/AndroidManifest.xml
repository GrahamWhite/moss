<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moss">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAdWaYuwOgZHhPFpo8k2flSNG1alMmnIqc"/>
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.Moss.NoActionBar" />

        <activity android:name=".CurrentSeshesActivity"
            android:exported="true"
            />

        <activity android:name=".CreateSeshActivity"
            android:exported="true"
            />

        <activity android:name=".MapPickerActivity"
            android:exported="true"
            />

        <activity android:name=".SeshMapActivity"
            android:exported="true"
            />
    </application>

</manifest>